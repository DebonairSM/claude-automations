# Required: shared secret VSol team members configure in their Claude Code MCP settings
MCP_API_KEY=change-me-to-a-long-random-string

# Required for private repos; omit only if the repo is public
GITHUB_TOKEN=github_pat_...

# Optional: pin to a specific branch (defaults to master)
# GITHUB_BRANCH=master
